# tocodex2 功能分析与实现方案

> 目标：把 sub2api 的管理工具 **tosub2**（`C:\Users\14527\Desktop\test\tosubV2`）改版为 **tocodex2**（本目录），
> 用于管理 **codex2api**（`C:\Users\14527\Desktop\test\codex2api`）后端。
>
> 本文档基于对三个项目源码的实际分析，是后续编码的实施依据。

---

## 目录

1. [三项目角色与关系](#1-三项目角色与关系)
2. [tosub2 功能与架构分析](#2-tosub2-功能与架构分析)
3. [tosub2 调用 sub2api 的完整接口清单](#3-tosub2-调用-sub2api-的完整接口清单)
4. [codex2api 管理端 API 清单（目标接口）](#4-codex2api-管理端-api-清单目标接口)
5. [核心差异对照表](#5-核心差异对照表)
6. [tocodex2 改版实现方案（逐模块）](#6-tocodex2-改版实现方案逐模块)
7. [关键逻辑适配点与决策](#7-关键逻辑适配点与决策)
8. [数据库 / 配置 / 环境变量改动](#8-数据库--配置--环境变量改动)
9. [前端改动清单](#9-前端改动清单)
10. [实施步骤与验证计划](#10-实施步骤与验证计划)
11. [风险与开放问题](#11-风险与开放问题)

---

## 1. 三项目角色与关系

| 项目 | 本地路径 | 角色 | 技术栈 |
|---|---|---|---|
| **sub2api** | `test\sub2api` | 旧后端：ChatGPT/Codex 账号池 → OpenAI 兼容 API | Go（backend/frontend） |
| **tosub2** (tosubV2) | `test\tosubV2` | sub2api 的管理工具（账号池运维控制台） | Node.js ≥20 / Fastify / React 19 / Vite / SQLite |
| **codex2api** | `test\codex2api` | 新后端：Codex/Grok/Claude/Antigravity 多渠道 → API 网关 | Go / Gin |
| **tocodex2** | `test\tocodex2`（本目录） | **待产出**：codex2api 的管理工具（tosub2 改版） | 同 tosub2 |

**管理关系**：tosub2 自身就是一个独立 Web 服务（默认 `127.0.0.1:1999`），本地 SQLite 存账号池；
通过 HTTP 调用 sub2api 的**管理端 API**（`x-api-key` 认证）完成「上传账号 / 巡检 / 修复 / 补号 / 代理更换」。
tocodex2 与 codex2api 的关系完全同构，只是把上游换成 codex2api 的 `/api/admin/*`（`X-Admin-Key` 认证）。

---

## 2. tosub2 功能与架构分析

### 2.1 总体架构

```
tosubV2/
├── server/               # Fastify 后端（npm workspace）
│   ├── index.js          # 入口：装配各模块
│   ├── core/             # v1 协议复用：ChatGPT 登录/Sentinel/TLS 指纹/Outlook 取件/接码
│   │                     #   （--json-events 事件流、chatgpt-credits.mjs 余额查询）
│   ├── lib/              # db(SQLite+迁移) / crypto(AES-256-GCM) / settings / sanitize /
│   │                     #   http-errors / upload-order / openai-fetch / totp / logger
│   ├── migrations/       # SQLite 迁移 0001~0012（PRAGMA user_version）
│   └── modules/
│       ├── auth/         # 首访设密、Cookie 会话、IP 限流、CSRF
│       ├── proxies/      # 本机代理池：导入去重、测活（curl_cffi TLS 指纹）、随机选路
│       ├── accounts/     # 三级号池（备用/主/废弃）+ Team 池：导入、登录、余额、
│       │                 #   批量上传远端、收编(adopt)、废弃快照（用量/代理/指纹收敛）
│       ├── jobs/         # 任务引擎：队列/并发、人工内联输入（验证码）、断点续跑、
│       │                 #   hooks（onTokensSaved/onLoginFinished 供 sub2api 模块挂载）
│       ├── sub2api/      # ★ 与远端后端对接的全部逻辑（本次改版核心）
│       ├── team/         # Team 号池：兑换、上传（复用 sub2api 上传语义）
│       ├── settings/     # 设置页后端
│       ├── dashboard/    # 仪表盘统计
│       └── static/       # 托管 web-dist 前端产物
└── web/                  # React 19 + Vite + TanStack Router/Query + Tailwind 4
    └── src/pages/        # dashboard / proxies / pools(reserve|main|discard|team) /
                          #   jobs / settings / sub2api / login
```

### 2.2 功能总览（改版后全部保留）

| 模块 | 能力 | 是否涉及上游 API |
|---|---|---|
| 认证 | 首访设密 / Cookie 30 天会话 / IP 限流 / 改密登出 | 否 |
| 代理池（本机） | 批量导入、测活、随机选路、失败直连 | 否（测活直连目标站） |
| 备用号池 | Outlook 四段导入、邮件初始化余额、批量入主池 | 否（微软官方接口） |
| 主号池 | 邮箱验证码自动登录、批量授权、批量余额、**批量上传远端** | **是** |
| 废弃号池 | 五类废弃原因、**远端用量/出口代理/指纹收敛快照**、移回主池 | **是** |
| 任务中心 | 队列/并发、人工输入、增量日志、取消重试、断点续跑 | 否 |
| **远端管控页** | 连接配置、分组/代理查看、监控巡检、一键换代理 IP、同步远端状态 | **是（核心）** |
| 安全 | 凭据/token/代理 AES-256-GCM 加密入库、日志脱敏 | 否 |

### 2.3 `server/modules/sub2api/` 内部结构（改版核心）

| 文件 | 职责 | 关键出口 |
|---|---|---|
| `client.js` | sub2api admin API HTTP 客户端（`x-api-key`，120s 超时）；账号邮箱/用量/错误信息/限流态的**形状适配函数** | `listGroups / listProxies / createProxy / deleteProxiesBatch / bulkUpdateAccounts / listAllOpenAiAccounts / findAccountByEmail / getAccount / getAccountStats / createAccountsBatch / updateAccount / clearError / setSchedulable / accountEmail / accountUsedAmount / accountErrorMessage / accountRateLimit / testConnection` |
| `upload.js` | 上传管线：串行闸门 → 远端 email 索引 → 新增/替换分流 → 创建前二次校验 → 最少绑定代理分配 → 余额 `---N` 后缀 → 回填 `sub2api_account_id`；`mergeUploadOptions` 上传默认项 | `uploadAccounts(ids, options)` |
| `monitor.js` | 巡检（默认 5 分钟）：同步远端状态 → error 分类（封禁关键词→邮件辅证→废弃；限流超阈值→废弃；临时错误→自动修复 refresh/login，连败熔断暂停待重授）→ 可选刷余额 → 自动补号（count/resource 两口径） | `runCheck / view / recentLogs / pushRepairedCredentials / noteRepairOutcome` |
| `remote-sync.js` | 远端同步：email/ID 索引（60s TTL 缓存）、回填关联、重复账号检测、余额查询的**远端代理选路**、废弃瞬间远端事实快照（出口代理+指纹收敛档位） | `syncRemoteStatus / resolveSub2apiProxy / resolveDiscardRemote` |
| `proxy-replace.js` | 一键更换代理 IP：粘贴 `ip:port:user:pass` → 建（名字纯数字自增）→ 旧代理账号洗牌均分改绑 → 删旧 | `replaceProxies({text, protocol, delete_old})` |
| `index.js` | 模块装配：装饰器注入（`app.sub2apiClient/Uploader/RemoteSync/Monitor`）、路由 `/api/v1/sub2api/*`、引擎 hooks 挂载 | — |

### 2.4 与远端相关的**本地数据**（SQLite `accounts` 表列）

- `sub2api_account_id` / `sub2api_status` / `sub2api_uploaded_at` / `sub2api_synced_at` —— 远端关联与镜像
- `discard_used_amount(_at/_source)` —— 废弃时远端累计用量快照
- `discard_proxy_(name/user/id/at)` —— 废弃时出口代理快照
- `discard_codex_fingerprint_mode(_at)` —— 废弃时指纹收敛档位快照
- `team_accounts.sub2api_account_id / sub2api_uploaded_at` —— Team 池关联

---

## 3. tosub2 调用 sub2api 的完整接口清单

认证方式：`x-api-key: <admin_key>` 请求头；基址 `config.base_url`。

| # | 用途 | 方法 + 路径 | 请求/响应要点 |
|---|---|---|---|
| 1 | 连接测试 / 分组列表 | `GET /api/v1/admin/groups/all?platform=openai` | 返回分组数组（`data` 或裸数组）；`testConnection` 用它测时延 |
| 2 | 代理列表 | `GET /api/v1/admin/proxies/all[?with_count=true]` | `{data:[{id,name,protocol,host,port,username,status,account_count}]}` |
| 3 | 创建代理 | `POST /api/v1/admin/proxies` | body `{name,protocol,host,port,username?,password?}`；响应含 `id` |
| 4 | 批量删代理 | `POST /api/v1/admin/proxies/batch-delete` | `{ids:[...]}`；仍有绑定的代理**跳过不删** |
| 5 | 账号全量（分页） | `GET /api/v1/admin/accounts?page=N&page_size=100&platform=openai[&status=]` | `{data:{items:[...],pages:N}}`；客户端循环翻页聚合 |
| 6 | 单账号 | `GET /api/v1/admin/accounts/{id}` | 账号完整对象（`credentials.email` 提取邮箱） |
| 7 | 账号统计 | `GET /api/v1/admin/accounts/{id}/stats?days=90` | `summary.total_cost` 等累计消费（废弃用量/预估余额数据源） |
| 8 | 批量创建 | `POST /api/v1/admin/accounts/batch`（头 `Idempotency-Key`） | `{accounts:[{name,platform,type:'oauth',credentials:{access_token,refresh_token,id_token,chatgpt_account_id,email},extra:{...},status,schedulable,group_ids,proxy_id,concurrency,load_factor,priority}]}`；响应不含新 ID |
| 9 | 更新账号（换凭据） | `PUT /api/v1/admin/accounts/{id}` | `{credentials:{...}}` —— 修复回推新 AT/RT 用 |
| 10 | 清除错误 | `POST /api/v1/admin/accounts/{id}/clear-error` | 空 body |
| 11 | 调度开关 | `POST /api/v1/admin/accounts/{id}/schedulable` | `{schedulable:bool}` —— 废弃/待重授时暂停，修复后恢复 |
| 12 | 批量更新 | `POST /api/v1/admin/accounts/bulk-update` | `{account_ids:[...], proxy_id:N}` —— 一键换代理 IP 的改绑 |

**客户端形状适配函数**（sub2api 账号对象 → 标准化）：

- `accountEmail`：`credentials.email → extra.email → name 正则`
- `accountUsedAmount`：候选表 `used_amount / consumed_amount / total_cost / cost / usage.* / billing.* / usage_stats.summary.total_cost`
- `accountErrorMessage`：`error_message / last_error / message / error.message` 拼接
- `accountRateLimit`：`rate_limited_at + rate_limit_reset_at`（OAuth 号限流**不写** `status=error`）

---

## 4. codex2api 管理端 API 清单（目标接口）

> 源码：`codex2api/admin/handler.go`（`RegisterRoutes`，行 1063–1369）。
> 认证：`X-Admin-Key: <ADMIN_SECRET>`（或 `Authorization: Bearer`）；未配置 secret 时 fail-closed（503，需先走 bootstrap）。
> 所有管理路由前缀 **`/api/admin`**。以下只列 tocodex2 会用到的。

### 4.1 分组（account-groups）

| 用途 | 接口 | 说明 |
|---|---|---|
| 分组列表 | `GET /api/admin/account-groups` | `{groups:[{id,name,description,color,sort_order,base_concurrency_override,member_count,proxy_urls,channel,created_at,updated_at}]}`；**按 `channel==='codex'` 过滤** |
| 建/改/删分组 | `POST/PATCH/DELETE /api/admin/account-groups[/:id]` | tocodex2 只读使用 |

### 4.2 账号（channel=codex）

| 用途 | 接口 | 请求/响应要点 |
|---|---|---|
| **分页列表** | `GET /api/admin/accounts?view=page&page=N&page_size≤500&channel=codex&search=&status=&sort=&order=` | 响应 `{accounts:[accountResponse],page,page_size,total,summary,facets,snapshot_at,stats_state}` |
| **轻量列表** | `GET /api/admin/accounts?view=lite&channel=codex` | `{accounts:[{id,name,email,plan_type,status,enabled,proxy_url,at_only,...}]}` —— 建 email/代理索引用，负载最小 |
| 单账号 | `GET /api/admin/accounts/:id` | 完整 `accountResponse`（含 detail 字段） |
| **创建账号** | `POST /api/admin/accounts` | `{name, refresh_token, session_token?, proxy_url?, custom_headers?, allow_duplicate?, skip_refresh?, group_ids?}`；`refresh_token` 支持**多行批量**（≤100）；响应 `{success,updated,duplicate,failed,bound_groups,group_ids,created_ids:[...]}` |
| 文件导入 | `POST /api/admin/accounts/import`（multipart） | `format=txt/json/json_at/at_txt` + `file` + `proxy_url/group_ids/...`（tocodex2 不用） |
| **单账号调度配置** | `PATCH /api/admin/accounts/:id/scheduler` | 见 4.3 白名单；`DisallowUnknownFields` |
| **批量更新** | `POST /api/admin/accounts/batch-update` | `{ids:[...]|selector, enabled?, locked?, + 4.3 全部字段}`；同样 `DisallowUnknownFields` |
| 启用/禁用 | `POST /api/admin/accounts/:id/enable` | `{enabled:bool}` |
| 锁定 | `POST /api/admin/accounts/:id/lock` | `{locked:bool}` |
| **重置状态（清错）** | `POST /api/admin/accounts/:id/reset-status` | 无 body；清冷却/模型冷却，恢复 active |
| 刷新 AT | `POST /api/admin/accounts/:id/refresh`、`POST /accounts/batch-refresh` | 用存量 RT 换新 AT |
| 删除 | `DELETE /api/admin/accounts/:id`（回收站）、`DELETE /:id/purge`、`POST /:id/restore`、`POST /accounts/batch-delete` | — |
| **账号用量** | `GET /api/admin/accounts/:id/usage?days=N` | `AccountUsageDetail{total_requests,total_tokens,total_account_billed,total_user_billed,avg_daily_*,...}` —— **累计消费字段为 `total_account_billed`** |
| 模型白名单 | `PATCH /api/admin/accounts/:id/models` | 模型列表 |
| 连通测试 | `GET /accounts/:id/test`、`POST /accounts/batch-test` | 可选 |

### 4.3 `updateAccountSchedulerReq` 字段白名单（batch-update 同样适用）

```
score_bias_override(-200..200)   base_concurrency_override(≥1)   skip_warm_tier
allowed_api_key_ids[]            tags[]                          group_ids[]
auto_pause_5h_threshold(0~1)     auto_pause_7h_threshold(0~1)
auto_pause_5h_disabled           auto_pause_7d_disabled
ignore_usage_limit_status_override  dispatch_count_limit          scheduler_priority
proxy_url                        custom_headers                  codex_fingerprint_mode
claude_* (Claude 专用)           timezone                        upstream_request_id_header
```

> ⚠️ `POST /accounts/batch-update` 与 `PATCH /accounts/:id/scheduler` 均 `DisallowUnknownFields`，
> 未知字段直接 400 —— 客户端必须只发白名单内字段。

### 4.4 代理（URL 实体模型）

| 用途 | 接口 | 请求/响应要点 |
|---|---|---|
| 代理列表 | `GET /api/admin/proxies` | `{proxies:[{id,url,label,enabled,bound_count,...}]}` —— **账号与代理通过 `proxy_url` 字符串关联**，不是代理 ID |
| **创建代理** | `POST /api/admin/proxies` | `{urls:["http://user:pass@host:port",...], label?}` → `{message,inserted,total}` —— **不返回新代理 ID**，需重拉列表按 URL 匹配 |
| 更新代理 | `PATCH /api/admin/proxies/:id` | `{url?,label?,enabled?}`；改 URL 自动把旧 URL 上的账号改绑到新 URL |
| 删除代理 | `DELETE /api/admin/proxies/:id` | **先解绑仍引用该 URL 的账号**再删 |
| 批量删除 | `POST /api/admin/proxies/batch-delete` | `{ids:[...]}` → `{deleted,unbound}` |
| 测活 | `POST /proxies/test`、`/proxies/test-all`、`/proxies/clean-error`、`/proxies/auto-balance` | tocodex2 可选 |

### 4.5 账号对象关键字段（`accountResponse`，channel=codex）

```
id, name, email, plan_type, status, error_message, enabled, locked,
group_ids[], proxy_url, codex_fingerprint_mode(off/device/session/full),
at_only, base_concurrency_override, base_concurrency_effective,
score_bias_override, score_bias_effective, scheduler_priority,
usage_percent_5h / usage_percent_7d, reset_5h_at / reset_7d_at,
credits_balance / credits_has_credits, last_used_at, created_at/updated_at
```

**运行时状态枚举**（`auth.Account.RuntimeStatus()`）：

```
active | error | rate_limited | usage_exhausted | unauthorized(≈封禁)
      | cooldown | quota_paused | refreshing | (冷却原因字符串)
```

---

## 5. 核心差异对照表

| 维度 | sub2api | codex2api | tocodex2 适配 |
|---|---|---|---|
| 认证头 | `x-api-key` | `X-Admin-Key`（或 Bearer） | client 统一改头 |
| 路由前缀 | `/api/v1/admin` | `/api/admin` | client 统一改前缀 |
| 渠道过滤 | `?platform=openai` | `?channel=codex` | 全部列表/搜索请求带 |
| 分组 | `groups/all`（平台参数） | `account-groups`（响应带 `channel`，需过滤 codex） | `listGroups` 改造 |
| 代理实体 | `{id,name,protocol,host,port,username,password}`，账号绑 `proxy_id` | `{id,url,label,enabled,bound_count}`，账号绑 **`proxy_url` 字符串** | 上传分配/换 IP/废弃快照全链路改 URL 维度 |
| 创建代理 | 单个建，返回 `id` | 批量 `{urls}`，**不返回 id**（重拉匹配）；名字概念弱化为 `label` | `createProxy` 改造 |
| 删代理 | 有绑定则跳过 | 删除时**自动解绑**账号 | 换 IP 流程必须**先改绑后删**（现有顺序已满足） |
| 创建账号 | `accounts/batch`，完整对象（AT/RT/邮箱全带）+ `Idempotency-Key` | `accounts`，**只收 `refresh_token`(+`session_token`)**；邮箱/AT/plan 由 codex2api 首次刷新回填；响应直接给 `created_ids` | 上传管线重构；幂等头废弃（改靠 RT 去重） |
| 服务端查重 | 无（客户端 email 索引查重） | **RT/ST 原文路由键查重**；同凭据重加且旧号 error 态 → 复活旧号 | 客户端 email 索引保留 + 服务端查重兜底 |
| 更新凭据 | `PUT accounts/:id {credentials}` | **无对应 API** | 修复回推改为「删旧 + 重加」（或 RT 未轮换时 `reset-status`+`refresh`） |
| 清错 | `clear-error` | `reset-status` | 改路径 |
| 调度开关 | `schedulable {schedulable}` | `enable {enabled}` | 改路径+字段 |
| 批量更新 | `bulk-update {account_ids, proxy_id}` | `batch-update {ids, proxy_url, ...}`（严格白名单） | 改路径+字段+白名单 |
| 限流语义 | `status=error` 之外用 `rate_limited_at/rate_limit_reset_at` | 状态枚举 `rate_limited / usage_exhausted` + `reset_5h_at/reset_7d_at` | `accountRateLimit` 重写 |
| 封禁语义 | 错误文本关键词（deactivated/banned/...） | 状态 `unauthorized`（healthTier banned） | 巡检封禁判定加状态维度 |
| 邮箱 | `credentials.email` 提取 | `email` 平铺字段；**`skip_refresh=true` 时可能为空**（未刷新过） | `accountEmail` 改直读 + name 兜底解析 |
| 累计用量 | `/stats summary.total_cost` 候选表 | `/usage?days=N → total_account_billed`（另 `usage_5h/7d_detail.account_billed`） | `accountUsedAmount` 重写 |
| 并发/优先级 | `concurrency / load_factor / priority` | `base_concurrency_override / score_bias_override(-200~200) / scheduler_priority` | 上传默认项映射 |
| 指纹收敛 | `extra.codex_fingerprint_mode`（创建时可写） | 平铺字段 `codex_fingerprint_mode`；**创建时取系统默认**，事后 PATCH 可改 | 上传默认档位 → 创建后 `batch-update` 补写 |
| 自动暂停 | `extra.auto_pause_5h/7d_disabled` | `auto_pause_5h/7d_disabled`（scheduler 字段） | 上传默认 → 创建后补写 |
| 长上下文计费 | `extra.openai_long_context_billing_enabled` | 无对应 | **功能移除** |

---

## 6. tocodex2 改版实现方案（逐模块）

### 6.1 `server/modules/codex2api/client.js` —— 全量重写

```
createCodex2apiClient(getConfig)
  request(endpoint, options)          # X-Admin-Key 头；错误 → CODEX2API_UNAVAILABLE / 504 超时
  listGroups()                        # GET /api/admin/account-groups → 过滤 channel==='codex'
  listProxies()                       # GET /api/admin/proxies → {proxies}（bound_count 恒带）
  createProxies(urls, label)          # POST /api/admin/proxies {urls,label} → 重拉列表按 URL 解析出 {id,url}
  deleteProxiesBatch(ids)             # POST /api/admin/proxies/batch-delete {ids}
  bulkUpdateAccounts({ids, patch})    # POST /api/admin/accounts/batch-update {ids, ...白名单字段}
  listAllAccounts()                   # GET /accounts?view=lite&channel=codex（轻量全量，无分页）→ 建索引用
  findAccountByEmail(email)           # GET /accounts?view=page&page_size=50&search=<email>&channel=codex
                                      #   → 精确匹配 email 字段（服务端搜索，取代翻页扫描）
  getAccount(id)                      # GET /api/admin/accounts/:id
  getAccountStats(id, days)           # GET /api/admin/accounts/:id/usage?days= → 取 total_account_billed
  createAccount(payload)              # POST /api/admin/accounts {name,refresh_token,session_token?,
                                      #   proxy_url,skip_refresh,group_ids,allow_duplicate}
                                      #   → {success,duplicate,failed,created_ids}
  deleteAccount(id)                   # DELETE /api/admin/accounts/:id
  resetStatus(id)                     # POST /api/admin/accounts/:id/reset-status
  setEnabled(id, enabled)             # POST /api/admin/accounts/:id/enable {enabled}
  refreshAccount(id)                  # POST /api/admin/accounts/:id/refresh
  accountEmail(account)               # account.email → name 内 oauth---<email>---N 正则兜底
  accountUsedAmount(account)          # total_account_billed / usage_*_detail.account_billed 候选表
  accountErrorMessage(account)        # error_message 直读
  accountRateLimit(account)           # status∈{rate_limited,usage_exhausted} → limited_now；
                                      #   reset_at = reset_5h_at || reset_7d_at
  accountConcurrency(account)         # base_concurrency_effective || base_concurrency_override
  testConnection()                    # GET /api/admin/account-groups → {ok, groups, latency_ms}
```

### 6.2 `upload.js` —— 载荷与回填重构

上传管线骨架（串行闸门、email 索引、创建前二次校验、余额后缀）**保留**，改动点：

1. **payload 构建**（`buildPayload` → codex2api 形状）：

```js
{
  name: `oauth---${email}---${balance}`,   // 保留命名约定（巡检 name 兜底解析依赖它）
  refresh_token: tokens.refresh_token,
  session_token: tokens.session_token || undefined,
  proxy_url: <最少绑定选出的代理 URL>,        // 不再是 proxy_id
  skip_refresh: options.skip_refresh !== false,  // 默认 true，防批量上传打爆上游
  allow_duplicate: false,
  ...(options.group_ids?.length ? { group_ids: options.group_ids } : {})
}
```

2. **创建后补写**（codex2api 创建时不收这些字段，统一创建成功后一次 `batch-update`）：
   - `codex_fingerprint_mode`（≠off 时）
   - `base_concurrency_override`（上传默认并发）
   - `score_bias_override`（余额分档默认：≤10→+40 / 11-19→+20 / 20-39→+30 / ≥40→+10，正向=优先调度）
   - `auto_pause_5h_disabled / auto_pause_7d_disabled`
   - 模型白名单 → 逐账号 `PATCH /accounts/:id/models`（保留功能）

3. **回填简化**：创建响应直接含 `created_ids`（与请求顺序一致），不再需要「重拉远端索引按 email 匹配」；
   索引重拉仅作为响应缺字段时的降级路径。

4. **替换凭据（重复号/修复回推）**：`deleteAccount(remoteId)` → `createAccount(新 RT + 原 name/proxy_url/group_ids)`
   → 回填新 `codex2api_account_id`；RT 未轮换的轻路径：`resetStatus + setEnabled(true)`。

5. **上传默认项**（`mergeUploadOptions`）字段变更：

| tosub2 字段 | tocodex2 字段 | 说明 |
|---|---|---|
| `concurrency` | `base_concurrency` | 语义等价（每号并发上限） |
| `priority` | `score_bias` | 范围 -200~200，余额分档自动填 |
| `load_factor` | ❌ 移除 | codex2api 无此概念 |
| `enable_long_context_billing` | ❌ 移除 | codex2api 无此概念 |
| `model_whitelist` | `model_whitelist`（保留） | 落地方式改 PATCH models |
| `codex_fingerprint_mode` | 保留 | 改为创建后补写 |
| `disable_auto_pause_5h/7d` | 保留 | 改为创建后补写 |
| `auto_select_proxy / proxy_id` | `auto_select_proxy / proxy_url` | 代理改 URL 维度 |
| —（新增） | `skip_refresh` | 默认 true |

### 6.3 `monitor.js` —— 状态语义适配

巡检骨架（同步、分类、辅证、修复、熔断、补号两口径、日志回执）**保留**，改动点：

| 逻辑点 | sub2api 行为 | tocodex2 行为 |
|---|---|---|
| 全量拉取 | `listAllOpenAiAccounts()` 分页 | `listAllAccounts()`（`view=lite` 单次全量，含 status/enabled/proxy_url/email） |
| OAuth 过滤 | `type==='oauth'` | 删除（channel=codex 即 OAuth；`at_only` 号照样跟踪） |
| 限流判定 | `accountRateLimit()`（rate_limited_at） | 同名函数重写：`status∈{rate_limited,usage_exhausted}`；`reset_at=reset_5h_at‖reset_7d_at` 超阈值废弃 |
| 封禁判定 | 错误文本正则命中 → 邮件辅证 | 状态 `unauthorized` **或** 错误文本正则命中 → 邮件辅证（辅证逻辑不变） |
| 临时错误修复 | status=error → refresh/login | status=error → 同样 refresh/login 修复链 |
| 修复回推 | `updateAccount(credentials)+clearError+setSchedulable(true)` | `pushRepairedCredentials` 改「删旧+重加」（先 `getAccount` 取原 name/proxy_url/group_ids） |
| 暂停远端调度 | `setSchedulable(id,false)` | `setEnabled(id,false)` |
| resource 口径并发 | `remote.concurrency` | `accountConcurrency(remote)`（base_concurrency_effective） |
| 分组过滤 | `inMonitoredGroups`（group_ids/account_groups） | 同逻辑，读平铺 `group_ids` |
| 补号两口径 | count / resource | **不变**（纯本地逻辑 + 上传管线） |

### 6.4 `remote-sync.js` —— 简化

| 函数 | 改动 |
|---|---|
| `buildAccountIndex` | 不变（email 索引 + 最小 id + 重复检测） |
| `syncRemoteStatus` | 列名改 `codex2api_*`；远端 status 直接平铺字段 |
| `resolveCodex2apiProxy`（原 resolveSub2apiProxy） | **大幅简化**：远端账号自带 `proxy_url`，直接返回，无需代理表二次解析 |
| `extractRemoteProxy` | 重写：解析 `proxy_url` 字符串 → `{host,port,username}`；name/label 查代理列表（60s 缓存）按 URL 匹配 |
| `extractCodexFingerprintMode` | 简化：直接读平铺字段 `codex_fingerprint_mode`；**字段存在且空/缺省即 off**（codex2api 恒回填该字段，语义比 sub2api 的 extra 键缺失简单） |
| `fetchDiscardRemote` | 不变（getAccount → findAccountByEmail 两档回退） |

### 6.5 `proxy-replace.js` —— URL 维度重写

流程顺序保留（解析粘贴 → 建新 → 改绑 → 删旧），改动点：

1. 解析行 → 直接构造完整 URL `protocol://user:pass@host:port`；
2. 与现有代理比对用 **URL 全等**（替代 host|port|user|pass 四元组）；
3. 新代理批量创建 `createProxies(urls, label)`（label 用纯数字自增命名续接，保持可读）→ 重拉列表按 URL 解析 id；
4. 改绑目标从「待删代理 id 集合」改为「待删代理 **URL** 集合」：`listAllAccounts()` 过滤 `proxy_url ∈ 待删 URL`；
5. 每组改绑 `bulkUpdateAccounts({ids, proxy_url: 新URL})`；
6. **先改绑后删**（codex2api 删除会自动解绑，顺序颠倒会裸奔）——现有实现已满足；
7. 删除结果统计：`batch-delete` 响应直接给 `{deleted, unbound}`，不再依赖重拉对比。

### 6.6 `discard-usage.js` —— 用量源替换

- `fetchUsedAmount`：`getAccountStats(id, 90)` → codex2api `/usage?days=` → `total_account_billed`
  （候选表同步收窄为 `total_account_billed / usage_5h_detail.account_billed / usage_7d_detail.account_billed`）；
- `resolveTargetRemote`：`getAccount` / `findAccountByEmail`（服务端 search）两档回退保留；
- `writeProxySnapshot` / `writeCodexFingerprintSnapshot`：字段来源换新 `extractRemoteProxy/extractCodexFingerprintMode`，落库列名不变。

### 6.7 其他后端模块

| 模块 | 改动 |
|---|---|
| `accounts/index.js` | 批量上传路由（`/accounts/batch-upload-sub2api` → `-codex2api`）调新 uploader；`buildMainBalanceEstimate`（主池预估余额）数据源换 `/usage`；收编（adopt_remote）远端查重换新 client；`join_auto_upload` 配置键名跟随 |
| `accounts/import.js` | `parseSub2apiAccountsExport` 保留（导入旧 sub2api 导出文件仍可用，仅命名空间调整） |
| `accounts/pools.js` | 废弃钩子传递的 remote 对象形状适配 |
| `team/upload.js` | 与主上传器同一套改动（RT-only 载荷 + 删旧重加替换） |
| `jobs/engine.js` | `balanceProxyResolver` 注释与日志文案改名（`sub2api 代理 #id` → `codex2api 代理 <url 摘要>`） |
| `dashboard/index.js` | 统计标签文案 |
| `settings/index.js` | 无 sub2api 专属逻辑（配置在 codex2api 模块内） |
| `lib/http-errors.js` | `sub2apiNotConfigured/sub2apiUnavailable` → `codex2apiNotConfigured/codex2apiUnavailable`；错误码 `SUB2API_*` → `CODEX2API_*` |
| `lib/upload-order.js` / `lib/settings.js` | 注释与键名跟随 |

---

## 7. 关键逻辑适配点与决策

### 7.1 决策一：`skip_refresh` 默认 true + name 兜底解析邮箱

**问题**：codex2api 添加账号只收 RT；邮箱在首次刷新后由 JWT/上游回填。若 `skip_refresh=false`，
批量上传会拉起无上限上游刷新（codex2api 自己的注释明确警告「每批 50 个裸 RT 会把网关打卡」）。

**决策**：上传默认 `skip_refresh=true`（可配置）。邮箱索引的兜底：
`accountEmail()` = `account.email` ‖ `name 中 oauth---<email>---N 正则`。
刚上传未刷新的号靠 name 命中；codex2api 后台刷新调度器随后回填 email，两路收敛。

### 7.2 决策二：凭据修复回推 = 删旧 + 重加

**问题**：sub2api 的 `PUT {credentials}` 在 codex2api 没有等价接口；RT 每次重登会轮换，
codex2api 的 RT 原文查重拦不住「新 RT + 同一邮箱」。

**决策**：`pushRepairedCredentials` / 上传替换流：
1. `getAccount(remoteId)` 快照 `name / proxy_url / group_ids / enabled`；
2. `deleteAccount(remoteId)`；
3. `createAccount({name, refresh_token: 新RT, proxy_url, group_ids})`；
4. 回填新 `codex2api_account_id`。
RT 未轮换的轻路径（仅清错复活）：`resetStatus(id) + setEnabled(id, true)`。
**代价**：codex2api 侧账号 id 变化，其本地用量统计/事件历史不迁移 —— 可接受（tocodex2 自己持有
email 维度的用量快照）。

### 7.3 决策三：代理选路全链路 URL 化

上传分配（最少绑定）、换 IP、废弃快照、余额任务选路统一以 `proxy_url` 为主键；
`bound_count` 代替「遍历账号数 proxy_id 计数」，选路计算从 O(账号数) 降为 O(代理数)。

### 7.4 决策四：严格字段白名单

`batch-update` / `scheduler` 均为 `DisallowUnknownFields`。client 层提供
`SCHEDULER_PATCH_FIELDS` 常量表，`bulkUpdateAccounts` 入参做白名单过滤，防止 400。

### 7.5 内部 API 面（tocodex2 自身路由）保持形状、整体改名

| tosub2 | tocodex2 |
|---|---|
| `GET/PUT /api/v1/sub2api/config` | `/api/v1/codex2api/config` |
| `POST /api/v1/sub2api/test` | `/api/v1/codex2api/test` |
| `GET /api/v1/sub2api/groups` | `/api/v1/codex2api/groups` |
| `GET /api/v1/sub2api/proxies` | `/api/v1/codex2api/proxies` |
| `POST /api/v1/sub2api/proxies/replace` | `/api/v1/codex2api/proxies/replace` |
| `GET /api/v1/sub2api/remote-accounts` | `/api/v1/codex2api/remote-accounts` |
| `POST /api/v1/sub2api/sync-remote` | `/api/v1/codex2api/sync-remote` |
| `GET/POST /api/v1/sub2api/monitor[/logs|/check]` | `/api/v1/codex2api/monitor/...` |
| `POST /api/v1/accounts/batch-upload-sub2api` | `POST /api/v1/accounts/batch-upload-codex2api` |

---

## 8. 数据库 / 配置 / 环境变量改动

### 8.1 SQLite 迁移（全新安装，直接改 0001~0012 源文件列名）

| 旧列/键 | 新列/键 |
|---|---|
| `accounts.sub2api_account_id` | `codex2api_account_id` |
| `accounts.sub2api_status / sub2api_uploaded_at / sub2api_synced_at` | `codex2api_status / ...` |
| `team_accounts.sub2api_*` | `codex2api_*` |
| settings 键 `sub2api.config` | `codex2api.config` |
| 迁移文件名 `0004_sub2api_remote_sync.sql` | `0004_codex2api_remote_sync.sql`（user_version 序号不变） |

### 8.2 环境变量 / 品牌

| 旧 | 新 |
|---|---|
| `TOSUB2_DATA_DIR / TOSUB2_PORT / TOSUB2_HOST / TOSUB2_SECRET_KEY / TOSUB2_CONSOLE_PASSWORD / TOSUB2_FORCE_SECURE_COOKIE / TOSUB2_PYTHON / TOSUB2_TLS_PROFILE / TOSUB2_LOG_LEVEL` | `TOCODEX2_*`（同名语义） |
| package name `tosub2` / 镜像 `tosubv2` | `tocodex2` |
| 默认端口 1999 | 改为 **2026**（tosub2 占 1999，两套控制台可同机并存） | 

### 8.3 codex2api 侧前置条件（用户须知）

- `ADMIN_SECRET` 已配置（未配置时管理接口 fail-closed）；
- tocodex2 配置页填：`base_url`（如 `http://127.0.0.1:8080`）+ `admin_key`（= ADMIN_SECRET）；
- 建议在 codex2api 系统设置里确认「新账号默认指纹收敛档位」预期值。

---

## 9. 前端改动清单

| 文件 | 改动 |
|---|---|
| `web/src/pages/sub2api.tsx` → `codex2api.tsx` | 页面整体改名；上传默认表单：并发→基础并发、优先级→调度偏置(-200~200)、删 load_factor/长上下文计费、增 skip_refresh；代理选择器 id→URL |
| `web/src/api/index.ts` | `sub2apiApi` → `codex2apiApi`，路径 `/sub2api/*` → `/codex2api/*`，`batch-upload-sub2api` → `-codex2api` |
| `web/src/api/types.ts` | `Sub2Api*` 类型 → `Codex2Api*`；`Account.initial_balance_source` 枚举值 `sub2api_name_suffix` → `codex2api_name_suffix`；`AccountExportFormat 'sub2api'` 保留（导入兼容） |
| `web/src/router.tsx` / `lib/nav.ts` / sidebar / mobile-navigation | 路由 `/sub2api` → `/codex2api`，导航项「Sub2API」→「Codex2API」 |
| `pages/pools/main.tsx` | 「上传 sub2api」按钮/结果文案、远端状态列 |
| `pages/pools/discard.tsx` | 用量来源/代理快照/指纹收敛列的 tooltip 文案 |
| `pages/pools/team.tsx` / `dashboard.tsx` / `jobs.tsx` | 文案与徽标 |
| `components/codex-fingerprint-mode-select.tsx` | 文案（档位值 off/device/session/full 两边一致，**无需改值**） |
| `hooks/use-hotkeys.ts` | 快捷键导航目标 |

---

## 10. 实施步骤与验证计划

### 步骤

1. **复制骨架**：tosubV2 → tocodex2（排除 `node_modules/.git/data/tmp/.playwright-mcp/web-dist`，node_modules 从 tosubV2 复制以保证离线可装可构建）。
2. **全局品牌改名**（脚本化，分大小写变体）：
   `sub2api→codex2api`、`Sub2Api/Sub2API→Codex2Api/Codex2API`、`SUB2API→CODEX2API`、
   `tosub2→tocodex2`、`TOSUB2→TOCODEX2`、`toSub2→toCodex2`；
   目录 `modules/sub2api→modules/codex2api`、页面文件、迁移文件名。
   ⚠️ 例外：`parseSub2apiAccountsExport`（旧导出文件导入兼容）、`PreviewSub2APIAccounts`（codex2api 自身接口名）等「引用外部既定名称」处需回改。
3. **重写 client.js**（§6.1）。
4. **适配五个核心文件**：upload / monitor / remote-sync / proxy-replace / discard-usage（§6.2–6.6）。
5. **accounts / team / engine / dashboard / http-errors 跟随**（§6.7）。
6. **迁移 SQL 列名**（§8.1）。
7. **前端改名与表单适配**（§9）。
8. **单测适配**：`sub2api-*.test.js → codex2api-*.test.js`，mock 上游响应体改为 codex2api 形状（`{accounts}/{proxies}/{groups}`、`created_ids`、`total_account_billed`、`X-Admin-Key`）。
9. **验证**（见下）。

### 验证清单

| 项 | 命令/方法 | 通过标准 |
|---|---|---|
| 语法 | `npm run check` | server+web 无错 |
| 单测 | `npm test` | 全绿（含改造后的 codex2api mock 用例） |
| 构建 | `npm run build` | web-dist 产出 |
| 启动 | `npm start`（默认端口 2026） | 首访设密可用 |
| 连通（真实实例） | 配置页「测试连接」 | 返回分组数与时延 |
| 上传链路 | 主池选 1 号上传 | codex2api 出现同名账号、本地回填 `codex2api_account_id`、指纹/并发补写生效 |
| 巡检 | 「立即巡检」 | 日志能分类 error/限流/封禁；修复回推走删旧重加 |
| 换 IP | 粘贴代理文本执行 | 新代理建立、账号改绑、旧代理删除 |
| 废弃快照 | 废弃 1 号 | 用量/出口代理/收敛档位三列落库 |

---

## 11. 风险与开放问题

| # | 风险/问题 | 影响 | 对策 |
|---|---|---|---|
| 1 | `skip_refresh=true` 时新号无 email，email 索引短暂失明 | 上传查重窗口期依赖 name 兜底 | name 命名约定强制保留 `oauth---<email>` 前缀；codex2api 后台刷新后自然回填 |
| 2 | 删旧+重加使 codex2api 账号 id 变化 | codex2api 侧历史统计断代 | tocodex2 本地持有 email 维度快照；轻路径（reset-status）优先 |
| 3 | `batch-update` 白名单严格 | 字段拼错整批 400 | client 层白名单常量 + 单测覆盖 |
| 4 | codex2api 代理创建不返回 id | 换 IP 需重拉列表 | 已计入流程（多一次列表请求） |
| 5 | codex2api 多渠道（grok/claude/antigravity）账号 | channel=codex 过滤漏放会误管他人账号 | 所有列表/搜索强制 `channel=codex`（client 单点收口） |
| 6 | 并存部署端口冲突 | 与 tosub2 同机跑会抢 1999 | 默认端口改 2026 |
| 7 | 旧 sub2api 导出文件导入 | 兼容性 | `parseSub2apiAccountsExport` 保留 |

---

*文档完 · 生成于 2026-09-15 · 基于源码分析：tosubV2(server/modules/*, lib/*, web/src/*)、codex2api(admin/handler.go, accounts_paged.go, account_response_builder.go, account_groups.go, token_credentials.go, auth/store.go, database/postgres.go)*
